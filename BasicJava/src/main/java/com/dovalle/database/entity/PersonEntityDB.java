package com.dovalle.database.entity;

import com.dovalle.classes.Person;
import com.dovalle.database.base.EntityDatabase;

public class PersonEntityDB extends EntityDatabase {

    public void add(Person person) {
        super.add(person);
    }
}
